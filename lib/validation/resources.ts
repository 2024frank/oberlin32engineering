import { z } from 'zod'
export const resourcePublishSchema=z.object({title:z.string().min(1).max(180),description:z.string().max(4000).default(''),category:z.string().max(120).default(''),sourceName:z.string().max(180).default(''),url:z.string().url().or(z.literal('')).default(''),sourceKind:z.enum(['club','official','external']).default('club'),officialSource:z.boolean().default(false),sourceUrl:z.string().url().or(z.literal('')).default(''),pinned:z.boolean().default(false),sortOrder:z.number().int().min(0).max(10000).default(100)}).superRefine((v,ctx)=>{if(v.officialSource&&v.sourceKind==='club')ctx.addIssue({code:'custom',path:['officialSource'],message:'Club-authored guidance cannot be official'});if(v.officialSource&&!v.sourceUrl)ctx.addIssue({code:'custom',path:['sourceUrl'],message:'Official sources require a source URL'})})
