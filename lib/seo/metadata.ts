import type { Metadata } from 'next'
import type { PageSnapshot } from '@/lib/page-builder/types'
import { getCmsRenderContext, getPublicSiteSettings } from '@/lib/page-builder/publicPages'
export async function metadataForCmsPage(page:PageSnapshot):Promise<Metadata>{const base=(process.env.NEXT_PUBLIC_SITE_URL??'https://oberlinengineeringclub.org').replace(/\/$/,'');const path=page.slug==='home'?'':`/${page.slug}`;const settings=await getPublicSiteSettings();const rawTitle=page.seoTitle||page.title;const title=settings.seo.titlePattern.includes('%s')?settings.seo.titlePattern.replace('%s',rawTitle):rawTitle;let image:string|undefined;const mediaId=page.ogMediaId??settings.seo.defaultOgMediaId;if(mediaId&&process.env.NEXT_PUBLIC_SUPABASE_URL){const context=await getCmsRenderContext();image=context.media?.[mediaId]?.url}return{title,description:page.seoDescription||undefined,alternates:{canonical:`${base}${path}`},openGraph:{title,description:page.seoDescription||undefined,url:`${base}${path}`,siteName:'Oberlin Engineering Club',type:'website',images:image?[{url:image}]:undefined}}}
