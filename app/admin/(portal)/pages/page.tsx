import Link from 'next/link';import { listAdminPages } from '@/lib/page-builder/pageService'
export default async function PagesIndex(){const pages=await listAdminPages();return <main className="admin-panel"><div className="admin-page-heading"><div><p className="eyebrow">Website</p><h1>Pages</h1><p>Public page structure and content live here instead of in React files.</p></div></div><div className="manager-list">{pages.map((p:any)=><article key={p.id}><div><strong>{p.page_drafts?.title??p.slug}</strong><small>/{p.slug==='home'?'':p.slug} · {p.published_version_id?'Published':'Not published'}</small></div><span>{p.page_drafts?.updated_at?new Date(p.page_drafts.updated_at).toLocaleString():''}</span><Link href={`/admin/pages/${p.slug}`}>Edit page →</Link></article>)}</div></main>}
