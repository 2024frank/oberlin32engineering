import { notFound } from 'next/navigation';import { getPublishedEvent } from '@/lib/content/events'
export default async function EventPage({params}:{params:Promise<{slug:string}>}){const e:any=await getPublishedEvent((await params).slug);if(!e)notFound();return <><section className="detail-hero"><div className="shell"><p className="eyebrow">{e.event_type}</p><h1>{e.title}</h1><p>{e.summary}</p></div></section><section className="detail-body"><div className="shell detail-grid"><div className="prose"><h2>About this event</h2><p>{e.description}</p></div><aside className="detail-aside"><dl><dt>When</dt><dd>{new Date(e.start_at).toLocaleString()}</dd><dt>Organizer</dt><dd>{e.organizer_name}</dd><dt>Location</dt><dd>{e.location||e.access_details}</dd>{e.access_details&&<><dt>Access</dt><dd>{e.access_details}</dd></>}</dl>{e.registration_url&&<a className="button button--cardinal" href={e.registration_url} target="_blank" rel="noreferrer">Register ↗</a>}</aside></div></section></>}
