import type { Metadata } from 'next'
import { metadataForCmsPage } from '@/lib/seo/metadata'
import { getPublishedPageBySlug } from '@/lib/page-builder/publicPages'
import { DirectoryHero } from '@/components/public/DirectoryHero'
import Link from 'next/link';import { ProjectFilters } from '@/components/public/filters/ProjectFilters';import { listPublishedProjects,parseProjectFilters } from '@/lib/content/projects'
function toParams(obj:Record<string,string|string[]|undefined>){const p=new URLSearchParams();for(const[k,v]of Object.entries(obj))if(Array.isArray(v))v.forEach(x=>p.append(k,x));else if(v)p.set(k,v);return p}
export async function generateMetadata():Promise<Metadata>{return metadataForCmsPage(await getPublishedPageBySlug("projects"))}
export default async function ProjectsPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams;const filters=parseProjectFilters(toParams(raw));const projects=await listPublishedProjects(filters);return <><DirectoryHero slug={"projects"} eyebrow={"Projects"} title={"Build something with us."} description={"OEC projects give students a chance to work on engineering problems outside class. Our first project teams are forming for 2026–27."} imageSlug={"engineering-robotics-assembly"}/><section className="directory"><div className="shell"><ProjectFilters defaults={filters}/>{projects.length?<div className="card-grid">{projects.map((p:any)=><article className="content-card" key={p.id}><span className="status-pill">{String(p.status).replaceAll('_',' ')}</span><h2>{p.title}</h2><p>{p.summary}</p><div className="tag-row">{(p.disciplines??[]).map((d:string)=><span key={d}>{d}</span>)}</div><Link href={`/projects/${p.slug}`}>View project →</Link></article>)}</div>:<div className="empty-state"><h2>Our first project teams are forming.</h2><p>Projects can involve hardware, software, CAD, electronics, robotics, or testing. Propose one and it could be the first.</p><Link className="button button--cardinal" href="/get-involved?type=propose_project">Propose a project</Link></div>}</div></section></>}
