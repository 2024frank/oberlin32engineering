import Link from 'next/link'; import type { z } from 'zod'; import type { textImageSchema } from '@/lib/page-builder/schemas/content'; import type { PageRenderContext } from '@/lib/page-builder/types'
export function TextImageSection({section,context}:{section:z.infer<typeof textImageSchema>;context?:PageRenderContext}){const media=section.imageId?context?.media?.[section.imageId]:undefined;return <section className="cms-section"><div className={`shell split-section split-section--${section.layout}`}><div>{section.eyebrow&&<p className="eyebrow">{section.eyebrow}</p>}<h2>{section.heading}</h2><p>{section.body}</p>{section.cta&&<Link className="text-link" href={section.cta.href}>{section.cta.label} →</Link>}</div><div className="media-frame">{media?<img src={media.url} alt={section.imageAlt||media.alt}/>:<div className="engineering-pattern" aria-hidden="true"/>}</div></div></section>}
