import type { z } from 'zod'; import type { gallerySchema } from '@/lib/page-builder/schemas/content'; import type { PageRenderContext } from '@/lib/page-builder/types'
export function GallerySection({section,context}:{section:z.infer<typeof gallerySchema>;context?:PageRenderContext}){return <section className="cms-section"><div className="shell">{section.heading&&<h2>{section.heading}</h2>}<div className="gallery-grid">{section.images.map(image=>{const media=context?.media?.[image.mediaId];return <figure key={image.mediaId}>{media?<img src={media.url} alt={image.alt}/>:<div className="media-placeholder"/>}{image.caption&&<figcaption>{image.caption}</figcaption>}</figure>})}</div></div></section>}
