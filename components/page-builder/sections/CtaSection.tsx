import Link from 'next/link'; import type { z } from 'zod'; import type { ctaSchema } from '@/lib/page-builder/schemas/cta'
export function CtaSection({section}:{section:z.infer<typeof ctaSchema>}){return <section className={`cms-section cta-section cta-section--${section.tone}`}><div className="shell"><div>{section.eyebrow&&<p className="eyebrow">{section.eyebrow}</p>}<h2>{section.heading}</h2><p>{section.body}</p></div><div className="button-row"><Link className="button button--light" href={section.primaryCta.href}>{section.primaryCta.label}</Link>{section.secondaryCta&&<Link className="button button--ghost" href={section.secondaryCta.href}>{section.secondaryCta.label}</Link>}</div></div></section>}
