import type { z } from 'zod'; import type { opportunityListSchema } from '@/lib/page-builder/schemas/community'; import type { PageRenderContext } from '@/lib/page-builder/types'
export function OpportunityListSection({section,context}:{section:z.infer<typeof opportunityListSchema>;context?:PageRenderContext}){const items=(context?.opportunities??[]).slice(0,section.limit) as any[];return <section className="cms-section cms-section--soft"><div className="shell"><h2>{section.heading}</h2><div className="card-grid">{items.map(o=><article className="content-card" key={o.id}><p className="eyebrow">{o.opportunity_type}</p><h3>{o.title}</h3><p>{o.organization}</p>{o.url&&<a href={o.url} target="_blank" rel="noreferrer">Open opportunity ↗</a>}</article>)}</div></div></section>}
