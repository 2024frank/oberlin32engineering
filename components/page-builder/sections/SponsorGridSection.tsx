import type { z } from 'zod'; import type { sponsorGridSchema } from '@/lib/page-builder/schemas/community'; import type { PageRenderContext } from '@/lib/page-builder/types'
export function SponsorGridSection({section,context}:{section:z.infer<typeof sponsorGridSchema>;context?:PageRenderContext}){const items=(context?.sponsors??[]).slice(0,section.limit) as any[];return <section className="cms-section"><div className="shell"><h2>{section.heading}</h2><div className="sponsor-grid">{items.map(s=><a key={s.id} href={s.url||'#'} target={s.url?'_blank':undefined} rel={s.url?'noreferrer':undefined}>{s.name}</a>)}</div></div></section>}
