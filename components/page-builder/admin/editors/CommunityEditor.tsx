'use client';import type { PageSection } from '@/lib/page-builder/types'
export function CommunityEditor({section,onChange}:{section:PageSection;onChange:(s:PageSection)=>void}){const s:any=section;const set=(k:string,v:unknown)=>onChange({...s,[k]:v});return <div className="section-editor-fields"><label>Eyebrow<input value={s.eyebrow??''} onChange={e=>set('eyebrow',e.target.value)}/></label><label>Heading<input value={s.heading??''} onChange={e=>set('heading',e.target.value)}/></label><label>Maximum items<input type="number" min="1" max="12" value={s.limit??4} onChange={e=>set('limit',Number(e.target.value))}/></label>{'currentOnly'in s&&<label className="check-field"><input type="checkbox" checked={Boolean(s.currentOnly)} onChange={e=>set('currentOnly',e.target.checked)}/>Current leaders only</label>}{'upcomingOnly'in s&&<label className="check-field"><input type="checkbox" checked={Boolean(s.upcomingOnly)} onChange={e=>set('upcomingOnly',e.target.checked)}/>Upcoming events only</label>}{'featuredOnly'in s&&<label className="check-field"><input type="checkbox" checked={Boolean(s.featuredOnly)} onChange={e=>set('featuredOnly',e.target.checked)}/>Featured only</label>}</div>}
