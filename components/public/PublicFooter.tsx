import Link from 'next/link'
import { BrandLogo } from '@/components/brand/BrandLogo'
export function PublicFooter({contactEmail='engineering@oberlin.edu',footerText='Build • Learn • Engineer Together',socialLinks={},badgeSrc}:{contactEmail?:string;footerText?:string;socialLinks?:Record<string,string>;badgeSrc?:string|null}){const social=Object.entries(socialLinks).filter(([,href])=>href);return <footer className="public-footer"><div className="shell footer-grid"><div><BrandLogo variant="badge" src={badgeSrc}/><p>{footerText}</p></div><div><strong>Oberlin Engineering Club</strong><p>A student-led home for engineering community, projects, opportunities, and the 3-2 pathway.</p>{social.length>0&&<div className="footer-social">{social.map(([label,href])=><a key={label} href={href} target="_blank" rel="noreferrer">{label[0].toUpperCase()+label.slice(1)}</a>)}</div>}</div><div><strong>Contact</strong><p><a href={`mailto:${contactEmail}`}>{contactEmail}</a></p><Link href="/get-involved">Get involved</Link></div></div></footer>}
