export type DirectoryFilterOptions = { disciplines:string[]; skills:string[]; majors:string[]; classYears:number[]; interests:string[] }
export function DirectoryFilters({ initial, options }: { initial: Record<string,string|undefined>; options: DirectoryFilterOptions }) {
  const Select=({name,label,values}:{name:string;label:string;values:(string|number)[]})=><label>{label}<select name={name} defaultValue={initial[name]??''}><option value="">All</option>{values.map(value=><option key={value} value={String(value)}>{value}</option>)}</select></label>
  return <form className="member-directory-filters" method="get"><label>Search<input name="q" defaultValue={initial.q??''} placeholder="Name, skill, discipline…"/></label><Select name="discipline" label="Discipline" values={options.disciplines}/><Select name="skill" label="Skill" values={options.skills}/><Select name="major" label="Major" values={options.majors}/><Select name="year" label="Class year" values={options.classYears}/><Select name="interest" label="Project interest" values={options.interests}/><label>Availability<input name="availability" defaultValue={initial.availability??''} placeholder="weekends"/></label><button>Filter members</button></form>
}
