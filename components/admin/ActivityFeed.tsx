export function ActivityFeed({items}:{items:Array<{id:string;action:string;entity_type:string;entity_id:string;created_at:string}>}){return <section className="admin-card"><div className="admin-card__heading"><h2>Recent activity</h2><p>Content and system actions recorded in the audit log.</p></div>{items.length?<ol className="activity-feed">{items.map(item=><li key={item.id}><span>{item.action.replaceAll('_',' ')}</span><div><strong>{item.entity_type.replaceAll('_',' ')}</strong><small>{item.entity_id}</small></div><time>{new Date(item.created_at).toLocaleString()}</time></li>)}</ol>:<p className="empty-copy">No recorded activity yet.</p>}</section>}
