'use client';import { Field,TextArea } from './fields';export function ProjectUpdateForm({value,onChange}:{value:any;onChange:(n:string,v:unknown)=>void}){return <div className="editor-form"><Field label="Project ID" name="projectId" value={value.projectId} onChange={onChange} required/><Field label="Title" name="title" value={value.title} onChange={onChange} required/><TextArea label="Summary" name="summary" value={value.summary} onChange={onChange}/><TextArea label="Body" name="body" value={value.body} onChange={onChange} rows={10}/><div className="form-grid"><Field label="Milestone" name="milestone" value={value.milestone} onChange={onChange}/><Field label="Update date" name="updateDate" value={value.updateDate} onChange={(n,v)=>onChange(n,v||null)} type="date"/></div></div>}
