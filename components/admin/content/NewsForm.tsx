'use client';import { Check,Field,TextArea } from './fields';export function NewsForm({value,onChange}:{value:any;onChange:(n:string,v:unknown)=>void}){return <div className="editor-form"><Field label="Title" name="title" value={value.title} onChange={onChange} required/><Field label="Slug" name="slug" value={value.slug} onChange={onChange}/><TextArea label="Excerpt" name="excerpt" value={value.excerpt} onChange={onChange}/><TextArea label="Body" name="body" value={value.body} onChange={onChange} rows={12}/><Field label="Author" name="author" value={value.author} onChange={onChange}/><Check label="Feature this post" name="featured" value={value.featured} onChange={onChange}/></div>}
