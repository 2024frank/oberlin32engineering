'use client';import { Check,Field,TextArea } from './fields';export function OpportunityForm({value,onChange}:{value:any;onChange:(n:string,v:unknown)=>void}){return <div className="editor-form"><Field label="Title" name="title" value={value.title} onChange={onChange} required/><div className="form-grid"><Field label="Organization" name="organization" value={value.organization} onChange={onChange}/><Field label="Type" name="opportunityType" value={value.opportunityType} onChange={onChange}/></div><TextArea label="Description" name="description" value={value.description} onChange={onChange}/><div className="form-grid"><Field label="Deadline" name="deadline" value={value.deadline} onChange={(n,v)=>onChange(n,v||null)} type="date"/><Field label="Location" name="location" value={value.location} onChange={onChange}/></div><Field label="URL" name="url" value={value.url} onChange={onChange} type="url" required/><Check label="Feature this opportunity" name="featured" value={value.featured} onChange={onChange}/></div>}
