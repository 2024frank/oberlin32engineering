import type { ReactNode } from 'react'
export type DataColumn<T>={key:string;header:string;cell:(row:T)=>ReactNode}
export function DataTable<T extends {id:string}>({rows,columns,empty='No records yet.',onRowHref,label='Records'}:{rows:T[];columns:DataColumn<T>[];empty?:string;onRowHref?:(row:T)=>string;label?:string}){if(!rows.length)return <div className="admin-table-empty">{empty}</div>;return <div className="admin-table-wrap"><table className="admin-table"><caption className="sr-only">{label}</caption><thead><tr>{columns.map(column=><th scope="col" key={column.key}>{column.header}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.id} data-href={onRowHref?.(row)}>{columns.map(column=><td key={column.key} data-label={column.header}>{column.cell(row)}</td>)}</tr>)}</tbody></table></div>}
