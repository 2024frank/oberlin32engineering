import type { DashboardSummary } from '@/lib/cms/dashboard'
const cards:[keyof DashboardSummary,string,string][]=[['pendingMemberApprovals','Member approvals','Verified & waiting'],['pendingProjectProposals','Project proposals','Needs decision'],['pendingProjectUpdateReviews','Team update reviews','Needs review'],['newSubmissions','New submissions','Needs review'],['drafts','Drafts awaiting publication','Saved, not public'],['upcomingEvents','Upcoming events','Published'],['activeProjects','Active projects','Published & active'],['closingOpportunities','Opportunities nearing deadline','Next 14 days'],['scheduledPublications','Scheduled publications','Pending'],['activeStaffInvites','Staff invitations','Super Admin only']]
export function DashboardCards({summary}:{summary:DashboardSummary}){return <div className="dashboard-cards">{cards.map(([key,label,note])=><article key={key}><span>{note}</span><strong>{summary[key]}</strong><h2>{label}</h2></article>)}</div>}
