import { render,screen } from '@testing-library/react';import { describe,expect,it } from 'vitest';import { DashboardCards } from '@/components/admin/DashboardCards'
describe('admin dashboard',()=>{it('renders operational metrics',()=>{render(<DashboardCards summary={{newSubmissions:3,drafts:2,upcomingEvents:1,activeProjects:4,closingOpportunities:2,scheduledPublications:1,pendingMemberApprovals:2,activeStaffInvites:1,pendingProjectProposals:3,pendingProjectUpdateReviews:2}}/>);for(const label of ['New submissions','Drafts awaiting publication','Upcoming events','Active projects','Opportunities nearing deadline','Scheduled publications','Member approvals','Project proposals','Team update reviews'])expect(screen.getByText(label)).toBeInTheDocument()})})
