import { describe,expect,it,vi } from 'vitest'
import { normalizeProjectProposalInput,reviewProjectProposal } from '@/lib/projects/proposals'
describe('project proposals',()=>{it('routes approval through one transactional dependency and returns the lead membership',async()=>{const approve=vi.fn(async()=>({projectId:'project-1',status:'APPROVED' as const,membership:{userId:'member-1',role:'LEAD' as const,status:'ACTIVE' as const}}));const reject=vi.fn();const result=await reviewProjectProposal({proposalId:'proposal-1',decision:'APPROVE',reviewerId:'admin-1'},{approve,reject:reject as never});expect(result).toMatchObject({projectId:'project-1',membership:{userId:'member-1',role:'LEAD',status:'ACTIVE'}});expect(approve).toHaveBeenCalledTimes(1);expect(reject).not.toHaveBeenCalled()});it('requires meaningful problem and goal statements',()=>expect(()=>normalizeProjectProposalInput({title:'Robot',problem:'short',goal:'also short'})).toThrow('PROJECT_PROBLEM_REQUIRED'))})
